# Exp4: Temporal Loss + L2-norm VICReg（对照组，预期坍缩）
_base_: v12_expB_inter01.yaml
experiment:
  name: exp4_l2_vicreg
  output_dir: /workspace/outputs/exp4_l2_vicreg
  seed: 47
training:
  epochs: 10
  consistency_weight: 0.0
  temporal_contrastive_weight: 0.1
  use_l2_space_vicreg: true
  save_every: 5
